# Include at the TLS-terminating edge only (CDN, load balancer or an nginx listening on 443).
# One year, subdomains included. Add `preload` only after confirming every subdomain serves HTTPS.
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
